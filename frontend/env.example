# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:5000

# Frontend URL (Optional - for embed codes in production)
NEXT_PUBLIC_FRONTEND_URL=http://localhost:3000

# Notes:
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local file to version control
# 3. NEXT_PUBLIC_ prefix is required for variables accessible in the browser
# 4. In production, set NEXT_PUBLIC_FRONTEND_URL to your deployed frontend URL

